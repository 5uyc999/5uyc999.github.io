# دليل رفع تحديث Lams — 16 سبتمبر 2026

> مهم: قاعدة البيانات أولاً، ثم رفع الواجهة. لا تعكس الترتيب.

## 1) قبل أي شيء — نسخة أمان من GitHub Desktop

1. افتح GitHub Desktop واختر مستودع Lams الحالي.
2. اضغط **Fetch origin** حتى تتأكد أن النسخة المحلية محدثة.
3. إذا عندك تغييرات محلية غير مرفوعة، اعمل لها Commit أو Stash/Backup قبل نسخ التحديث.
4. الأفضل إنشاء Branch مؤقت باسم مثل:
   `marketing-attribution-seo-2026-09-16`
   ثم اختبار النسخة قبل دمجها في `main`.

## 2) نفّذ تحديث Supabase قبل Deploy

1. افتح Supabase Dashboard للمشروع المستخدم في الموقع الحالي.
2. ادخل **SQL Editor** ثم **New query**.
3. افتح الملف:
   `supabase/migrations/20260916093000_lead_attribution_crm.sql`
4. انسخ محتواه كاملاً إلى SQL Editor واضغط **Run**.
5. بعد النجاح، من Table Editor تأكد من:
   - وجود الأعمدة الجديدة داخل `contact_inquiries` مثل `lead_status`, `sale_value`, `reference_code`, `first_source`, `first_gclid`.
   - وجود جدول جديد باسم `lead_interactions`.

سبب تنفيذ SQL أولاً: النماذج الجديدة ترسل أعمدة attribution جديدة؛ لو تم نشر الواجهة قبل Migration قد يفشل إرسال النماذج.

## 3) نسخ الملفات المعدلة إلى المستودع المحلي الحالي

1. في GitHub Desktop: **Repository > Show in Explorer**.
2. اترك مجلد المستودع الحالي مفتوحاً، ولا تحذف مجلد `.git` المخفي.
3. فك ضغط الحزمة المعدلة في مجلد منفصل.
4. انسخ محتويات الحزمة المعدلة إلى مجلد المستودع الحالي واختر **Replace files in destination** عند السؤال.
5. الحزمة لا تحتوي `.env`؛ اترك `.env` الموجود عندك كما هو.
6. احذف يدوياً الملفات القديمة التالية إن كانت ما زالت موجودة في المستودع:
   - `public/School_Sys.html`
   - `public/Mongazat.html`
   - `public/Order_Guard_V2_Documentation.html`
   - `public/Order_Guard_v2.0.2.zip`
   - `public/sitemap-main.xml`

## 4) فحص محلي قبل Commit

إذا Node.js وnpm موجودان على الجهاز، افتح Terminal داخل Repository وشغّل:

```bash
npm ci
npm test
npm run build
```

لو كلهم نجحوا، كمل. لو `npm ci` غير مناسب بسبب البيئة المحلية استخدم `npm install` ثم `npm test` و`npm run build`.

## 5) راجع التغييرات في GitHub Desktop

تأكد أن GitHub Desktop يعرض التغييرات الجديدة، خصوصاً:

- `src/lib/attribution.ts`
- `src/lib/leadTracking.ts`
- `src/lib/tracking.ts`
- `src/pages/admin/AdminInquiries.tsx`
- `src/pages/admin/AdminLeads.tsx`
- `src/pages/admin/AdminTracking.tsx`
- `src/pages/LocationService.tsx`
- `public/sitemap.xml`
- `public/robots.txt`
- `supabase/migrations/20260916093000_lead_attribution_crm.sql`
- `.gitignore`
- `.env.example`

ولا تسمح بإضافة `.env` إلى الـCommit.

## 6) Commit وPush من GitHub Desktop

اكتب Summary مثل:

`feat: add lead attribution CRM tracking and local SEO pages`

ثم:

1. **Commit to [اسم الفرع]**
2. **Push origin**
3. إذا استخدمت Branch تجريبي، اختبر Preview Deployment أولاً، ثم ادمجه في `main` وPush `main`.
4. إذا الاستضافة متصلة بـGitHub `main`، سيبدأ Deploy تلقائياً بعد Push/Merge.

## 7) اختبارات ما بعد الـDeploy

اختبر بالترتيب:

1. افتح الصفحة الرئيسية وContact وGet Quote.
2. افتح:
   - `/areas/riyadh`
   - `/areas/al-kharj`
   - `/areas/al-muzahimiyah`
3. افتح `/sitemap.xml` وتأكد أن الصفحات الثلاث موجودة.
4. افتح الموقع برابط اختبار مثل:
   `/?utm_source=test&utm_medium=cpc&utm_campaign=deployment_test`
5. اضغط WhatsApp وتأكد أن الرسالة بها `مرجع: LMS-...`.
6. افتح Tag Assistant واضغط اتصال مرة واحدة فقط. يجب أن ترى:
   - `click_phone` مرة واحدة
   - `conversion` مرة واحدة
   - `send_to = AW-18432449326/JUDECI6mjPIcEK62o9VE`
7. ادخل `/admin/tracking` وتأكد أن ضغط الاتصال/واتساب ظهر بالمصدر والمرجع.
8. أرسل نموذج اختبار من Contact أو Get Quote، ثم افتح `/admin/inquiries` وتأكد أن Source وCampaign وReference ظهرت.
9. غيّر حالة العميل التجريبي إلى `qualified` ثم `won` وأدخل `sale_value`. تأكد أن لوحة التحكم والتتبع يعرضان المبيعات والإيراد.
10. احذف بيانات الاختبار بعد التأكد.

## 8) إعداد Google Ads بعد الـDeploy

لا تغيّر الـCPC الآن؛ اترك Max CPC = **8 SAR** لمدة 48–72 ساعة كما تم الاتفاق.

أضف على مستوى Campaign **Final URL suffix** بدون علامة `?`:

`utm_source=google&utm_medium=cpc&utm_campaign=lams_search&utm_term={keyword}&utm_content={creative}`

واترك Auto-tagging مفعلاً حتى يتم حفظ `gclid` أيضاً.

## 9) Search Console بعد الـDeploy

1. أعد إرسال `https://lams.sooftit.com/sitemap.xml`.
2. استخدم URL Inspection للصفحات الثلاث الجديدة.
3. اطلب Indexing للصفحات التي ليست مفهرسة.

## 10) الاستخدام اليومي للـCRM

كل عميل حقيقي يجب تصنيفه داخل لوحة الإدارة:

- `new`: جديد
- `qualified`: يريد خدمة فعلية ضمن نطاق العمل
- `unqualified`: غير مناسب، مثل شخص يريد بيع أثاث
- `quoted`: تم إرسال عرض سعر
- `won`: حجز/بيع فعلي، مع تسجيل قيمة البيع
- `lost`: لم يتم إغلاق الصفقة

كلما كان التصنيف منتظماً أصبحت أرقام Leads → Qualified → Sales → Revenue دقيقة ويمكن لاحقاً إرسال Qualified/Won إلى Google Ads لتحسين المزايدة.
